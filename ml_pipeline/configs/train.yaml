# Runtime Training Configuration
# Model architecture and hyperparams are in model.yaml

training:
  data_dir: "data/processed/"
  checkpoint_dir: "checkpoints/"
  device: "auto"  # 'cuda', 'cpu', or 'auto'
  dry_run: false
  dry_run_samples: 4
  log_interval: 10
  
  # Optional overrides for hyperparams in model.yaml during specific runs
  # epochs: 50
  # batch_size: 32
