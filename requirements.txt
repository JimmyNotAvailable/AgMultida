# requirements.txt -- Minimal project-wide dependencies for Batch 1-3
# Only includes packages actually imported by existing code.
# Versions are lower-bounded, not strictly pinned, for flexibility.

# ----- Backend -----
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
PyJWT>=2.8.0

# ----- ML / DL -----
torch>=2.0.0
torchvision>=0.15.0
timm>=0.9.0

# ----- ONNX -----
onnx>=1.14.0
onnxruntime>=1.15.0
onnxscript>=0.1.0

# ----- AI System -----
scikit-learn>=1.3.0
joblib>=1.3.0
numpy>=1.24.0
pandas>=2.0.0
rasterio>=1.3.0

# ----- Config / Utils -----
pyyaml>=6.0
psycopg[binary,pool]>=3.2.0
alembic>=1.13.0
sqlalchemy>=2.0.0

# ----- Testing -----
pytest>=7.4.0
httpx>=0.24.0
